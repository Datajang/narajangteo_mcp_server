runtime: python
build:
  installCommand: pip install -e .
startCommand: python -m smithery.cli.start
configSchema:
  type: object
  properties:
    api_key:
      type: string
      title: "API Key"
      description: "나라장터 API 키 (data.go.kr 공공데이터포털에서 발급)"
  required:
    - api_key
